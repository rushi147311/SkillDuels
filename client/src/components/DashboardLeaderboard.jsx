export default function DashboardLeaderboard({ players }) {
  return <section className="dashboard-panel dashboard-list-panel"><div className="dashboard-section-heading"><div><span className="dashboard-eyebrow">Compete</span><h2>Leaderboard</h2></div><span className="heading-badge">Top 10</span></div><div className="leaderboard-list">{players.length ? players.map((player, index) => <div className="leaderboard-row" key={player.id}><b className={`rank rank-${index + 1}`}>{index + 1}</b><div className="mini-avatar">{player.username.slice(0, 1).toUpperCase()}</div><div className="leader-player"><strong>{player.username}</strong><span>Level {player.level} · {player.wins} wins</span></div><strong className="leader-xp">{player.xp} XP</strong></div>) : <p className="empty-dashboard">No registered players yet.</p>}</div></section>;
}